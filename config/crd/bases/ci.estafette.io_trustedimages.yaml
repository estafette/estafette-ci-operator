
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: trustedimages.ci.estafette.io
spec:
  group: ci.estafette.io
  names:
    kind: TrustedImage
    listKind: TrustedImageList
    plural: trustedimages
    singular: trustedimage
  scope: ""
  validation:
    openAPIV3Schema:
      description: TrustedImage is the Schema for the trustedimages API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: TrustedImageSpec defines the desired state of TrustedImage
          properties:
            allowCommands:
              type: boolean
            injectedCredentialTypes:
              items:
                type: string
              type: array
            path:
              description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                Important: Run "make" to regenerate code after modifying this file'
              type: string
            runDocker:
              type: boolean
            runPrivileged:
              type: boolean
            whitelistedPipelines:
              type: string
          required:
          - path
          type: object
        status:
          description: TrustedImageStatus defines the observed state of TrustedImage
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
