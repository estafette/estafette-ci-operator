
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: credentials.ci.estafette.io
spec:
  group: ci.estafette.io
  names:
    kind: Credential
    listKind: CredentialList
    plural: credentials
    singular: credential
  scope: ""
  validation:
    openAPIV3Schema:
      description: Credential is the Schema for the credentials API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: CredentialSpec defines the desired state of Credential
          properties:
            additionalProperties:
              type: object
            type:
              type: string
            whitelistedPipelines:
              type: string
            whitelistedTrustedImages:
              type: string
          required:
          - type
          type: object
        status:
          description: ClusterStatus defines the observed state of Cluster
          properties:
            configmap:
              description: The configmap bound to the Credential
              type: string
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
